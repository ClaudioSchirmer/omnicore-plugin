service: omnicore-gen-golden-host
relational:
  dialect: sqlserver
  dsn: "${DATABASE_URL:}"
  clock: db          # MANDATORY since framework v0.65.0 — no default; boot aborts without it.
migrations:
  dir: ./migrations/sqlserver
  autoRun: check
auth:
  mode: disabled
